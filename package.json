{
  "name": "vault",
  "version": "1.0.0",
  "scripts": {
    "build": "cd vault/client && npm install && npm run build",
    "start": "node vault/server/index.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
